
# Date: 11/14/25
# Author: Aoi | shadowsnowolf 
# Could use a while loop to keep alloctaing 
# until allotment points = 0
# ex: while allotment_points < 0:
# prompt 

allotment_points = 13
stats = {"St":1,"Ma":1,"En":1,
    "Ag":1,"Lu":1
}

print("Stats:")
for key in stats:
    print(key)

def display_allotment_points():
    print(

stat_choice = input("Enter a stat to allocate:" ).capitalize()
if stat_choice == "St":
    print(f"\nAvailable allotment points: {allotment_points}")
    st_allotment = int(input("\nEnter how many strength points you want to allocate:" ))
    if st_allotment > 0:
        allotment_points -= st_allotment 
        stats["St"] += st_allotment
        print("\nNew stats:")
        for key, value in stats.items():
            print(key, value)
        print("Available 
elif stat_choice == "Ma":
    pass
elif stat_choice == "En":
    pass
elif stat_choice == "Ag":
    pass
elif stat_choice == "Lu":
    pass